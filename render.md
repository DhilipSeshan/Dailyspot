# Render Deploy Configuration
# This file tells Render how to build and run your app.
# Upload this project to Render as a Web Service with these settings:
#
# Build Command: cd DailySpot && npm install
# Start Command: cd DailySpot && npm start
# Root Directory: (leave blank — repo root)
# Environment Variables (set in Render dashboard):
#   NODE_ENV=production
#   SESSION_SECRET=<your-random-secret>
#   TURSO_DATABASE_URL=<your-turso-url>
#   TURSO_AUTH_TOKEN=<your-turso-token>
#   ADMIN_EMAIL=admin@dailyspot.com
#   ADMIN_PASSWORD=<your-secure-password>
