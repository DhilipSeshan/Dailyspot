services:
  - type: web
    name: dailyspot
    runtime: node
    buildCommand: cd DailySpot && npm install && npm run seed
    startCommand: cd DailySpot && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: ADMIN_EMAIL
        value: admin@dailyspot.com
      - key: ADMIN_PASSWORD
        sync: false
